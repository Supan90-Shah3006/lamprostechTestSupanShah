#Program For Sum Of Two Elements
#Change Input According To Your Requirement

nums = [3, 3] #Your Input Array
target = 6 #Target Sum

for i in range(len(nums)): #As Per Length Of Array i will be decided
    for j in range(i + 1, len(nums)): #j will be decided according to i
        if (nums[j] == target - nums[i]): # if second number is (target value - first value) then return index 
            print([i, j])
                    
